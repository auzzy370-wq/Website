# VITRUS data folder

Reserved for future JSON payloads, CMS exports, workout plans, and nutrition libraries.

The current static prototype keeps its sample data in JavaScript files so the pages run without fetch requests.
